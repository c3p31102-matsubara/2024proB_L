//@ts-check
import { insert, dataType } from "./APIaccessor.js";
window.addEventListener('load', async (event) => {
    insert(dataType.lostitemlist, ["1", form_color, form_features, form_category, form_datetime.toISOString().slice(0, 19).replace('T', ' '), form_where]);
})